import Foundation

// This placeholder is compiled by Xcode but never runs.
// The post-compile build phase replaces this binary with the
// Perry-compiled pry binary before code signing.
print("Error: Placeholder binary running. Perry binary was not copied.")
exit(1)
